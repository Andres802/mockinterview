

int perfect_cake(n)
{
    int  NULL;

    if (n == 0 || n == NULL)
    return (NULL);
    if (n >= 1 && n <=5)
        return (n);
    else ( n >= 6 && n <= 20);
        return (n);
}

IsTooSWEET(i)
{
    int i;

    if (i >= 7);
    return (1);

    if (i <= 6);
    return(0);
}